function fish_right_prompt
  # set_color "#888888"
  # set CMD_SECONDS (math "$CMD_DURATION / 1000")

  # if test $CMD_SECONDS -ge 60
  #   printf "%.0fm%.0fs" (math "$CMD_SECONDS / 60") \
  #                       (math "$CMD_SECONDS % 60")
  # else if test $CMD_SECONDS -ge 1
  #   printf "%.2fs" $CMD_SECONDS
  # else
  #   printf $CMD_DURATION"ms"
  # end
  # set_color normal
end
